#include "memory.h"
#include "../include/arp_header.h" 
#include <string.h>


void pack_arp(eth_header* eth_head, arp_header* arp_head) {
  const count_of_bits = 42; 
  memcpy(eth_head*, arp_head, uint64_t); 

}
